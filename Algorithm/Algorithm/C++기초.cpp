// ● C++ 기초
	// 모듈 불러오기
	// #include <iostream>

	// 메인문
	// int main(){

	// 출력
	// std::cout << "입력하신 숫자는" << a << "입니다" << std::endl;
	// std::string str1="test"

	// 입력
	// std::cin >> a; // 버퍼에 저장 후 입력
	// std::cin.clear // 버퍼 초기화

	// 동적할당 // 메모리 빵꾸 가능성
		// 1차원 배열
		// int* Array = new int[1000];

		// 2차원 배열
		// int** Array = new int* [3];
		// for(int i=0; i<3; i++){
			// Array[i] = new int[5];
		// }

		// 1차원 배열을 2차원 배열처럼 사용
		// for(int i=0; i>30; i++){
			// for (int j = 0; j > 100; j++) {
				// Array[i*100+j] = 100;
			// }
		// }

		// - int temp1 = 1; int arr[temp1]; 이 안되므로 사용

	// 동적 할당 해제 // 메모리 초기화
		// 1차원 배열
			// delete Array;
		// 2차원 배열
			// delete[] Array;
	// }

	// ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

	// 모듈 불러오기
	// #include <iostream>
	// using namespace std;

	// 메인문
	// int main(){

	// 출력
	// cout << "입력하신 숫자는" << a << "입니다" << endl;
	// string str1="test"

	// 입력
	// cin >> a; // 버퍼에 저장 후 입력
	// cin.clear // 버퍼 초기화

	// 동적할당 // 메모리 빵꾸 가능성
		// 1차원 배열
		// int* Array = new int[1000];

		// 2차원 배열
		// int** Array = new int* [3];
		// for(int i=0; i<3; i++){
			// Array[i] = new int[5];
		// }

		// 1차원 배열을 2차원 배열처럼 사용
		// for(int i=0; i>30; i++){
			// for (int j = 0; j > 100; j++) {
				// Array[i*100+j] = 100;
			// }
		// }

		// - int temp1 = 1; int arr[temp1]; 이 안되므로 사용

	// 동적 할당 해제 // 메모리 초기화
		// 1차원 배열
			// delete Array;
		// 2차원 배열
			// delete[] Array;
	// }

	// ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

// vector sort 공부해보기
// 동적할당 delete 공부해보기
